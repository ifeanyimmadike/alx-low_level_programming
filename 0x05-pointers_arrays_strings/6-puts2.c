#include "main.h"
/**
 * puts2-output single character
 * @str : string
 * Return :0
 */
void puts2(char *str)
{
	int i;
	char out;

	for (i = 0 ; i < 0 ; i++)
	{
		str[i] = out;
		_putchar(out);
	}
	_putchar('\n');
}

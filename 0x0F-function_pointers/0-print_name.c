#include <stdio.h>
#include "main.h"
/**
 * print_name-function print name
 * @name:input name
 * @f:pointer
 */
void print_name(char *name, void (*f)(char *))
{
	printf("%s", name[j]);
	f(name);
}
int main(void)
{
	int i, j, n;
	for (i = 0; i < n; i++)
		for (j = 0; j < i; j++)
		{
			f(name[j]);
		}
}
